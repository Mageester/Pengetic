# Scope Schema

The scope package is a versioned YAML file that defines:

- primary domain
- base URL
- allowed subdomains
- allowed URLs
- out-of-scope assets
- allowed login areas
- allowed API areas
- tool allowlist
- rate limits
- testing window
- authorization note
- contacts

Wildcard entries are rejected. Only explicitly listed assets are considered in scope.

